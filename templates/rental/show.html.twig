{% extends 'base.html.twig' %}

{% block title %}Location{% endblock %}

{% block body %}
    <h1 class="text-center m-2">Votre location</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>StartDate</th>
                <td>{{ rental.startDate ? rental.startDate|date('d/m/Y H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Endtime</th>
                <td>{{ rental.endtime ? rental.endtime|date('d/m/Y H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('rental_index') }}">Retour</a>

    <a href="{{ path('rental_edit', {'id': rental.id}) }}">Editer</a>

    {{ include('rental/_delete_form.html.twig') }}
{% endblock %}
