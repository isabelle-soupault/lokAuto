<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}🚍 LokAuto 🚍{% endblock %}</title>
        {# Run `composer require symfony/webpack-encore-bundle`
           and uncomment the following Encore helpers to start using Symfony UX #}
        {% block stylesheets %}
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
            {#{{ encore_entry_link_tags('app') }}#}
        {% endblock %}

        {% block javascripts %}
            {#{{ encore_entry_script_tags('app') }}#}
        {% endblock %}
    </head>
    <body>
        <header>
        {% block header %}
            {#<nav>
            <div class="nav-wrapper">
                <div class="container">
                    <a href="{{ path('home') }}" class="brand-logo">Symfony Blog</a>
                        <ul id="nav-mobile" class="right hide-on-med-and-down">
                        <li><a href="{{ path('home') }}">Home</a></li>
                        {% if app.user %}
                            <li><a href="/logout">Logout</a></li>
                        {% else %}
                            <li><a href="/login">Login</a></li>
                        {% endif %}
                        </ul>
                    </div>
                </div>
            </nav>#}

        <nav class="navbar navbar-expand-lg bg-nav mb-5 container">
  <div class="container-fluid marge">
    <a class="navbar-brand link-dark fw-bold fs-1" href="{{ path('home') }}">LokAuto</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse justify-content-end " id="">
      <ul class="navbar-nav">
        <li class="nav-item">
          <a class="nav-link active link-info" aria-current="page" href="{{ path('home') }}">Accueil</a>
        </li>
        <li class="nav-item">
          <a class="nav-link link-info" href="{{ path('car_index') }}">Véhicules disponibles</a>
        </li>
        
        <li class="nav-item">
            {% if app.user %}
                {% if app.user and 'ROLE_ADMIN' in app.user.roles %}
                    <li class="nav-item">
                        <a class="nav-link link-info" href="{{ path('admin') }}">Tableau de bord</a>
                    </li>
                {% endif %}
                    <li class="nav-item"> <a class="nav-link link-info" href="{{ path('user_show', {id:app.user.id}) }}">Mon profil</a></li>
                    <li><a class="nav-link link-info" href="/logout">Se déconnecter</a></li>
                {% else %}
                <li><a class="nav-link link-info"href="/register">S'inscrire</a></li>
                    <li><a class="nav-link link-info"href="/login">Se connecter</a></li>
                    
                {% endif %}
        </li> 

      </ul>
    </div>
  </div>
</nav>

<span class="h1 container m-5" >Bonjour 
    {% if app.user %}
    {{ app.user.firstname }} 
    {% endif %}
!</span>

        {% endblock %}
    </header>
    
   <div class="container mainBody">{% block body %}{% endblock %}</div>



{#Footer             #}

        {% block footer %}
        <footer class=" footer text-center text-lg-start bg-nav">
        <!-- Copyright -->
        <div class="text-center p-3">
            © {{ "now"|date("Y") }} Copyright:
            <span class="text-dark" >Rachida Antoine et Iza</span>
        </div>
        <!-- Copyright -->

        
        </footer>
    {% endblock %}

    	{% block scripts %}
		<script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.4.0/jquery.min.js"></script>

	{% endblock %}
    </body>
</html>
